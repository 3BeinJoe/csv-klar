{
  "name": "csv-quality-checker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/build-site.mjs && node --test",
    "build": "node scripts/build-site.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
